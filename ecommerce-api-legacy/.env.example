# Porta HTTP da aplicação
PORT=3000
NODE_ENV=development

# Secrets — em produção são obrigatórios (a aplicação falha ao subir sem eles)
DB_USER=
DB_PASS=
PAYMENT_GATEWAY_KEY=
SMTP_USER=

# Token exigido pelas rotas administrativas (header x-admin-token)
ADMIN_TOKEN=dev-admin-token
